extends layout 
block content 
    h1 =title 
    if book_instances.length 
        p The following book instances are using this book:
        div(styple="margin-left:20px;margin-top:20px")
            h4 bookinstances 
            ul 
                for copy in book_instances 
                    li 
                        a(href=copy.url) #{copy._id}
    else 
        p Do you want to delete this book?
        form(method="POST")
            div.form-group
                input#bookid.form-control(name="bookid",type="hidden",value=book._id)
            button.btn.btn-danger(type="submit") Delete
            